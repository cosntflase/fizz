/* global Fizz */

Fizz.defineFragment('Fizz-Layout', function(element, socket) {
    var fragment = this;
    
    fragment.element = $(element);
    fragment.socket = socket;
    
    fragment.controller = function($scope) {
        
    };
    
});